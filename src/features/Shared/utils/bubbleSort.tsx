
export const bubbleSort = (array: Array<any>, sortkey: string) => {
    // JavaScript bubble sort
    // console.log ("Bubble Sorting on", sortkey);
    
    return array;
}

/*
const bubbleSort = (arr:any) => {
    var i, j;
    var len = arr.length;     
    var isSwapped = false;
    for(i =0; i < len; i++){
        isSwapped = false;
        for(j = 0; j < len; j++){
            // console.log (arr[j].meta.updated)
            if(arr[j] > arr[j + 1]){
                
                var temp = arr[j]
                arr[j] = arr[j+1];
                arr[j+1] = temp;
                isSwapped = true;
            }
        }
        if(!isSwapped) break;
    }
    return arr;
}
*/